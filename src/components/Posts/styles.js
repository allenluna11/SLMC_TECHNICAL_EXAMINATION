import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  table: {
    minWidth: 700,
    },
    container: {
        textAlign: "center",
        margin: "3%",
    },
    table: {
        marginTop: "3%",
    },  
    title: {
        fontWeight: "bold"
    },
});

export default useStyles;